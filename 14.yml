- name: check n
  hosts: localhost
  tasks:
  vars_prompt:
  - name: number
    prompt: Please enter the number
    private: no
  tasks:
  - name: number is less than 10
    ansible.builtin.debug:
      msg: "Given number is {{number}} is less than 10"
    when: number | int < 10    
   - name: number is less than 10
    ansible.builtin.debug:
      msg: "Given number is {{number}} is greter than 10"
    when: number | int > 10    